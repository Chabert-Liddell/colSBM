% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimatepopbm.R
\name{clusterize_bipartite_collection}
\alias{clusterize_bipartite_collection}
\title{Perform the clustering procedure}
\usage{
clusterize_bipartite_collection(
  networks_list,
  colsbm_model,
  net_id = NULL,
  distribution = "bernoulli",
  nb_run = 3L,
  global_opts = list(),
  fit_opts = list(),
  fit_init = NULL,
  full_inference = FALSE,
  save_path = fs::file_temp(pattern = "clustering", ext = "Rds")
)
}
\arguments{
\item{networks_list}{A list of matrices representing the networks to
partition}

\item{colsbm_model}{Which colBiSBM to use, one of "iid", "pi", "rho",
"pirho".}

\item{net_id}{A vector of string, the name of the networks.}

\item{distribution}{A string, the emission distribution, either "bernoulli"
(the default) or "poisson".}

\item{nb_run}{An integer, the number of run the algorithm do. Default to 3.}

\item{global_opts}{Global options for the outer algorithm and the output.
See details.}

\item{fit_opts}{Fit options for the VEM algorithm. See details}

\item{full_inference}{A boolean indicating if we should do the procedure
until all networks are in their own partition.}

\item{save_path}{A file path specifying where to save the current clustering.
Useful for long running clustering. Defaults to
\code{fs::file_temp(pattern = "clustering", ext = "Rds")}.}
}
\description{
Perform the clustering procedure
}
